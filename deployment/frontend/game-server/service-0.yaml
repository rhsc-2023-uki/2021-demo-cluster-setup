kind: Service
metadata:
  labels:
    app: app-name-0
    application: game-server
  name: app-name-0
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    statefulset.kubernetes.io/pod-name: app-name-0
